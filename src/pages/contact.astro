---
export const prerender = false;

import Base from "../layouts/Base.astro";

const FORM_ACTION = "https://formspree.io/f/xxxxabcd"; // <-- replace

const seoTitle = "Leadership Coaching Consultation | EzyCoaching";
const seoDescription =
  "Request a leadership coaching conversation focused on one real situation. No sales pitch. No obligation. Just clarity on what’s holding progress back.";
const canonical = "https://ezycoaching.com/contact/";

const faqJsonLd = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "What happens after I submit a request?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Every request is reviewed. If coaching could help, we’ll reach out to schedule a conversation. If not, we’ll let you know directly."
      }
    },
    {
      "@type": "Question",
      "name": "Is this a sales call?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "No. The conversation is a focused discussion about one real leadership situation and whether coaching is the right lever."
      }
    },
    {
      "@type": "Question",
      "name": "Who is this conversation for?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Leaders who are noticing recurring friction they can’t resolve alone and are ready to look at it directly."
      }
    }
  ]
};
---

<Base title={seoTitle} description={seoDescription}>
  <Fragment slot="head">
    <link rel="canonical" href={canonical} />
    <meta property="og:title" content={seoTitle} />
    <meta property="og:description" content={seoDescription} />
    <meta property="og:type" content="website" />
    <script type="application/ld+json" set:html={JSON.stringify(faqJsonLd)}></script>
  </Fragment>

  <main>
  <!-- SECTION 1 — ENTRY -->
<section class="section">
  <div class="container">
    <div class="text-col">
      <h1 data-reveal-item class="text-[color:var(--primary)] text-balance">
        Clarity doesn’t come from reading more.<br />
        It comes from deciding to engage.
      </h1>

      <p class="mt-6 text-[18px] leading-relaxed text-[color:var(--muted)]">
        Most leaders reach out when something keeps resurfacing and they’ve stopped ignoring it.
      </p>
    </div>
  </div>
</section>

<!-- SECTION — READINESS FRAMING -->
<section class="section">
  <div class="container">
    <div class="text-col">

      <!-- Anchor line -->
      <p
        data-reveal-item
        class="text-[color:var(--text)] font-medium"
      >
        This conversation is not for everyone.
      </p>

      <!-- Primary block (dominant) -->
      <div class="mt-6 space-y-4">
        <p class="text-[18px] leading-relaxed text-[color:var(--muted)]">
          This conversation is for leaders who are noticing friction they can’t resolve alone.
        </p>

        <p class="text-[18px] leading-relaxed text-[color:var(--muted)]">
          It’s for moments when something keeps resurfacing —<br />
          a decision you’re delaying,<br />
          a dynamic that isn’t shifting,<br />
          or a sense that things look fine but don’t feel aligned.
        </p>

        <p class="text-[18px] leading-relaxed text-[color:var(--muted)]">
          It’s a focused discussion about <span class="text-[color:var(--text)] font-medium">one real leadership situation</span>
          you’re currently facing,<br />
          and an opportunity to test whether coaching is the right lever.
        </p>
      </div>

      <!-- Secondary block (visually quieter) -->
      <div class="mt-10 pt-6 border-t border-[rgba(24,73,121,0.12)]">
        <p class="text-sm font-semibold text-[color:var(--text)]">
          This conversation is not:
        </p>

        <div class="mt-4 space-y-2 text-[18px] leading-relaxed text-[color:var(--muted)]">
          <p>A sales pitch.</p>
          <p>A free consultation.</p>
          <p>A general coaching overview.</p>
        </div>
      </div>

      <!-- Closing filter -->
      <p class="mt-8 text-[18px] leading-relaxed text-[color:var(--muted)]">
        If nothing specific comes to mind,<br />
        this may not be the right time.
      </p>

    </div>
  </div>
</section>

<!-- SECTION 4 — COMMITMENT QUESTION -->
<section class="section">
  <div class="container">
    <div class="text-col">
      <p
        data-reveal-item
        class="text-[11px] tracking-[0.22em] uppercase font-semibold text-[color:var(--accent)]"
      >
        Before you reach out
      </p>

      <h2 data-reveal-item class="mt-4 text-[color:var(--primary)] text-balance">
        Leaders who get value from this conversation usually have one situation they keep returning to.
      </h2>

      <p class="mt-6 text-[18px] leading-relaxed text-[color:var(--muted)]">
        It might be a decision they’re delaying.<br />
        A team dynamic that isn’t shifting.<br />
        Or a sense that things look fine, but don’t feel aligned.
      </p>

      <p class="mt-6 text-[18px] leading-relaxed text-[color:var(--muted)]">
        If nothing specific comes to mind, this may not be the right time.
      </p>
    </div>
  </div>
</section>

<!-- SECTION 5 — FORM (CONTAINED) -->
<section class="section">
  <div class="container">
    <div class="text-col">
      <div class="card p-7 md:p-10 ring-1 ring-[color:var(--border)] bg-[color:var(--surface)]">
        <p class="text-[18px] leading-relaxed text-[color:var(--text)] font-medium">
          If you’re ready to explore one real situation, leave your details below.
        </p>

        <form
          class="mt-8 grid grid-cols-1 md:grid-cols-2 gap-6"
          method="POST"
          action="https://formspree.io/f/xpqjwjar"
        >
          <!-- (optional but helpful) subject + page context in Formspree inbox -->
          <input type="hidden" name="_subject" value="EzyCoaching — Contact request" />
          <input type="hidden" name="source" value="/contact" />

          <div class="md:col-span-1">
            <label class="block text-sm font-semibold text-[color:var(--text)]" for="name">Name</label>
            <input
              id="name"
              name="name"
              autocomplete="name"
              minlength="3"
              required
              class="mt-2 w-full rounded-[var(--radius-md)] border bg-white/70 px-4 py-3 text-[color:var(--text)] outline-none focus:ring-4 focus:ring-[color:var(--focus)]"
              placeholder="Your name"
            />
          </div>

          <div class="md:col-span-1">
            <label class="block text-sm font-semibold text-[color:var(--text)]" for="email">Email</label>
            <input
              id="email"
              name="email"
              type="email"
              autocomplete="email"
              inputmode="email"
              required
              class="mt-2 w-full rounded-[var(--radius-md)] border bg-white/70 px-4 py-3 text-[color:var(--text)] outline-none focus:ring-4 focus:ring-[color:var(--focus)]"
              placeholder="name@company.com"
            />
          </div>

          <!-- Phone (optional, but recommended) -->
          <div class="md:col-span-1">
            <label class="block text-sm font-semibold text-[color:var(--text)]" for="phone">
              Mobile phone <span class="text-[color:var(--muted)] font-normal">(optional)</span>
            </label>
            <input
              id="phone"
              name="phone"
              type="tel"
              autocomplete="tel"
              inputmode="tel"
              pattern="^[0-9+()\-.\s]{7,20}$"
              class="mt-2 w-full rounded-[var(--radius-md)] border bg-white/70 px-4 py-3 text-[color:var(--text)] outline-none focus:ring-4 focus:ring-[color:var(--focus)]"
              placeholder="+65 9xxx xxxx"
            />
            <p class="mt-2 text-sm text-[color:var(--muted)]">
              Used only to coordinate scheduling if email is slow.
            </p>
          </div>

          <div class="md:col-span-1">
            <label class="block text-sm font-semibold text-[color:var(--text)]" for="role">Role / Organisation</label>
            <input
              id="role"
              name="role"
              autocomplete="organization-title"
              class="mt-2 w-full rounded-[var(--radius-md)] border bg-white/70 px-4 py-3 text-[color:var(--text)] outline-none focus:ring-4 focus:ring-[color:var(--focus)]"
              placeholder="Role, company"
            />
          </div>

          <div class="md:col-span-2">
            <label class="block text-sm font-semibold text-[color:var(--text)]" for="situation">
              One situation you’re currently navigating <span class="text-[color:var(--accent)]">*</span>
            </label>
            <p class="mt-2 text-sm text-[color:var(--muted)]">
              Be specific. What keeps resurfacing?
            </p>
            <textarea
              id="situation"
              name="situation"
              rows="5"
              required
              minlength="20"
              class="mt-3 w-full rounded-[var(--radius-md)] border bg-white/70 px-4 py-3 text-[color:var(--text)] outline-none focus:ring-4 focus:ring-[color:var(--focus)]"
              placeholder="What’s the decision / friction / dynamic you keep returning to?"
            ></textarea>
          </div>

          <div class="md:col-span-2 pt-2">
            <button type="submit" class="btn-primary inline-flex items-center gap-2">
              Request a conversation <span aria-hidden="true" class="text-lg leading-none">›</span>
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
</section>

<!-- SECTION 6 — WHAT HAPPENS NEXT -->
<section class="section">
  <div class="container">
    <div class="text-col">
      <!-- anchor line (system rule) -->
      <p class="text-[18px] leading-relaxed text-[color:var(--text)] font-medium">
        We review every request.
      </p>

      <p class="mt-4 text-[18px] leading-relaxed text-[color:var(--muted)]">
        If it looks like coaching could help, you’ll hear from us to schedule a conversation.
        If not, we’ll let you know directly.
      </p>

      <p class="mt-4 text-[18px] leading-relaxed text-[color:var(--muted)]">
        Either way, there’s no obligation.
      </p>
    </div>
  </div>
</section>

<!-- SECTION 7 — QUIET CLOSE -->
<section class="section">
  <div class="container">
    <div class="text-col">
      <!-- closing line should land darker + slightly stronger -->
      <p class="text-[20px] leading-relaxed text-[color:var(--text)] font-medium">
        Avoidance is often the first signal.<br />
        Clarity begins when you stop managing around it.
      </p>
    </div>
  </div>
</section>
  </main>
</Base>